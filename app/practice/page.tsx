"use client";import {useState} from "react";import SpeakButton from "../../components/SpeakButton";export default function Practice(){const [v,setV]=useState("");const [r,setR]=useState("");const answer="Ik woon in Amsterdam.";function check(){setR(v.trim().toLowerCase().replace(/[.!?]/g,"")===answer.toLowerCase().replace(/[.!?]/g,"")?"✓ Goed gedaan!":"Niet helemaal. Antwoord: "+answer)}return <main style={{maxWidth:700,margin:"60px auto",padding:24}}><a href="/">← DutchPass</a><h1>Daily Practice</h1><p>Schrijf in het Nederlands: “I live in Amsterdam.”</p><SpeakButton text={answer}/><div><input value={v} onChange={e=>setV(e.target.value)} style={{padding:12,width:"80%",marginTop:20}}/><button onClick={check} style={{padding:12}}>Controleer</button></div><p>{r}</p></main>}