"use client";import {useState} from "react";import {supabase} from "../../lib/supabase";export default function Login(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[msg,setMsg]=useState("");async function signUp(){const{error}=await supabase.auth.signUp({email,password});setMsg(error?error.message:"Account created. Check your email if confirmation is enabled.")}async function signIn(){const{error}=await supabase.auth.signInWithPassword({email,password});if(error)setMsg(error.message);else{setMsg("Signed in.");location.href="/dashboard"}}return <main style={{maxWidth:460,margin:"70px auto",padding:24}}><a href="/">← DutchPass</a><h1>Log in</h1><input placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} style={{display:"block",padding:12,width:"100%",margin:"10px 0"}}/><input type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)} style={{display:"block",padding:12,width:"100%",margin:"10px 0"}}/><button onClick={signIn} style={{padding:12,marginRight:10}}>Log in</button><button onClick={signUp} style={{padding:12}}>Create account</button><p>{msg}</p></main>}